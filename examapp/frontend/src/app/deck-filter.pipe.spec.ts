import { DeckFilterPipe } from './deck-filter.pipe';

describe('DeckFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DeckFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
